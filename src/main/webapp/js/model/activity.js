define(['backbone'], function(Backbone) {
    var ActivityModel = Backbone.Model.extend({

    	urlRoot : 'activity',
    	initialize : function(){
    		
    	}

    });
    return ActivityModel;
});